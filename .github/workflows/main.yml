on: [workflow_dispatch,push]
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - run: ls
      - run: echo "done"
      - name: Keep running
        run: |
          while true; do
            echo "Still running..."
            sleep 60
          done
